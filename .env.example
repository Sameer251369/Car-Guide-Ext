# Baked into the production build. No trailing slash, no /api/v1 suffix.
# Also set this in the Vercel project: Settings → Environment Variables.
VITE_API_URL=https://car-guide-engine.onrender.com

# Local `npm run dev` only: Vite proxy target (local Django or Render).
# VITE_PROXY_TARGET=http://127.0.0.1:8000
# VITE_PROXY_TARGET=https://car-guide-engine.onrender.com
