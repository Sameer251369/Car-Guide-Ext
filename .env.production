# Production uses same-origin /api and /media so vercel.json can proxy to Render.
# Do not set VITE_API_URL here or in the Vercel dashboard unless Django CORS
# allows the Vercel origin with credentials.
# VITE_API_URL=https://car-guide-engine.onrender.com
